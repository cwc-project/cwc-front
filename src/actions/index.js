export * from './todos';
export * from './projects';
export * from './user';

//project id
// export let project_id = null;
// export function pickProject(projects, index) {
//     const project = Object.assign({}, projects[index]);
//     project_id = project.id;
// };